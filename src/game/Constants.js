const CONSTANTS = {
  /**
   * origin (i.e. top left)
   * (A,1) -->
   * |
   * v
   *
   * north is up
   * south is down
   * east is right
   * west is left
   */
  DIR: Object.freeze({ north: 1, south: 2, east: 3, west: 4 }),
};

module.exports = CONSTANTS;
